Algebraic Data Types in Céu
===========================

- TODO
    - what are ADTs
    - alternative to C struct/union/enum
    - not polymorphic
    - typesafe

A simple `Point` ADT with fields `x` and `y`:

    data Point with
        var int x;
        var int y;
    end

